const app = require ('./index');

describe('GET request', () => {
    test('responds with all restaurants', () => {
        expect(app.get)
    })
})

describe('person objects', () => {
    test('have a name', () => {
        expect(person.name).toEqual("test person")
    })
})